interface:
  display_name: "GPT Orchestration Build"
  short_description: "Build every confirmed orchestration finding"
  default_prompt: "Use $gpt-orchestration-build with exact GPT-5.6 routes to turn these findings into verified implementation waves."
